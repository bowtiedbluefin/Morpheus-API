# Self-Contained Local Testing Environment
# Copy this to .env.local for completely local development
# Usage: docker-compose -f docker-compose.local.yml up --build

# Local Testing Mode (set to false to use Cognito authentication)
BYPASS_COGNITO_AUTH=true
LOCAL_TESTING_MODE=true

# CORS Configuration for local development
CORS_ALLOWED_ORIGINS=https://openbeta.mor.org,https://api.mor.org,http://localhost:3000,http://localhost:8080

# =============================================================================
# COGNITO AUTHENTICATION
# =============================================================================
# Replace these with your dev environment Cognito values
# COGNITO_USER_POOL_ID=
# COGNITO_CLIENT_ID=
# COGNITO_REGION=
# COGNITO_DOMAIN=

# Model Service (external - works fine)
ACTIVE_MODELS_URL=https://active.dev.mor.org/active_models.json
# Optional. Empty = sibling *_bids.json of ACTIVE_MODELS_URL.
# ACTIVE_BIDS_URL=https://active.dev.mor.org/active_bids.json
DEFAULT_FALLBACK_MODEL=mistral-31-24b

# Proxy Router (can use dev environment or mock locally)
#ROXY_ROUTER_URL=http://router.dev.mor.org:8082
PROXY_ROUTER_URL=http://localhost:9000
PROXY_ROUTER_USERNAME=admin
PROXY_ROUTER_PASSWORD=admin

# Local Development Settings
LOG_LEVEL=DEBUG
LOG_JSON=false
LOG_IS_PROD=false
ENVIRONMENT=local

# Log Levels (DEBUG, INFO, WARNING, ERROR)
# Example: Enable verbose logging for specific components during development
LOG_LEVEL=DEBUG              # Master control (all components inherit this if not defined)
# LOG_LEVEL_PROXY=DEBUG        # Debug session creation/proxy communication
# LOG_LEVEL_MODELS=DEBUG       # Debug model fetching and caching
# LOG_LEVEL_AUTH=WARN          # Reduce auth noise to warnings only
# LOG_LEVEL_API=DEBUG          # Debug chat completion and API endpoints
# LOG_LEVEL_CORE=WARN          # Reduce infrastructure noise

# Local Development Settings
ENVIRONMENT=local

# Optional: Mock proxy router for completely offline testing
# PROXY_ROUTER_URL=http://mock-router:8082
# MOCK_PROXY_ROUTER=true
